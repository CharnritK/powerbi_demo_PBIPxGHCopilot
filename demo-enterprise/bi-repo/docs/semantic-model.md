# Semantic Model Notes

The BI repo owns the semantic layer used by reports and business users.

For this demo, the semantic model:

- consumes curated CSV-backed sample data
- defines relationships and measures in TMDL
- supports a small report shell for presentation purposes

In a real setup, the source would typically be curated tables or views published by Data Engineering.
