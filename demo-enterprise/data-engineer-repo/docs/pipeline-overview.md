# Pipeline Overview

The mocked pipeline story is intentionally simple:

1. ingest source drops into bronze
2. standardize and conform into silver
3. publish curated sales-serving objects into gold

This is enough to explain upstream ownership during a demo without implying a full production platform.
