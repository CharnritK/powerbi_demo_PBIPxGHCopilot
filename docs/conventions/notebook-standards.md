# Notebook Standards

- Keep notebooks ordered and audience-readable.
- Import from `src/` or documented wrappers; do not embed core logic.
- Use markdown to explain what is being run and why.
- Avoid storing secrets or large binary outputs in notebooks.
- When notebook behavior becomes reusable, move it into `src/`.
