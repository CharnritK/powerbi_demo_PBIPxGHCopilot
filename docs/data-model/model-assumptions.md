# Model Assumptions

- Sample data is intentionally local and committed for demo repeatability.
- `DataRootFolder` points to the committed `assets/data/` folder.
- The primary workflow assumes import-mode sample data.
- Service principal `executeQueries` support may still be limited by dataset features such as RLS or SSO.
- Validation parity between TEST and PROD indicates consistency, not absolute business truth.
