# Presentation Assets

This folder is reference-only. It preserves speaker materials, slide assets, and session scripts from the original demo-oriented repository shape.

Selected repo-path references in these documents are kept aligned with the current repository layout, but the folder remains presentation-oriented rather than operationally authoritative. Use the root `README.md` and `docs/README.md` for current setup and engineering guidance.
