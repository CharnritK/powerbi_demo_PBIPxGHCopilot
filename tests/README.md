# Tests

Tests are optional in the minimum viable package. In a polished version, add:
- unit tests for config loading
- mocked API tests for workspace, dataset, and DAX query calls
- notebook smoke test execution in CI
